# NEXUS HQ — Classic Edition v1.2.0 (پورت 8787)
نسخه‌ی کلاسیک با همه‌ی درخواست‌های جدید:

| فایل | حجم | توضیح |
|---|---|---|
| `NEXUS-HQ-Classic-Setup.exe` | ۹۵ MB | ویندوز — بار اول نصب می‌شود (شورتکات/Uninstaller) و برنامه در `http://127.0.0.1:8787` بالا می‌آید |
| `NEXUS-HQ-Classic-1.2.0-android.apk` | ۰.۵ MB | اندروید — طراحی موبایل با نوار پایین |

## 🆕 تغییرات v1.2.0
- **پورت: 8765 → 8787**
- **ریسپانسیو کامل**: فرم‌ها و پنجره‌ها دیگر نصفه نمی‌مانند — مودال‌ها max-height + اسکرول داخلی + safe-area موبایل
- **تم شیشه‌ای (Glass/Acrylic)** پیش‌فرض روشن + **تم تیره/روشن/خودکار** از تنظیمات
- **خروج = ذخیره روی ابر**: موقع بستن برنامه، داده به‌صورت خودکار/با تأیید روی GitHub Gist شما ذخیره می‌شود (توکن از تنظیمات ← همگام‌سازی ابری)
- **قفل PIN مثل تلگرام**: تنظیمات ← قفل برنامه — رمز ۴ تا ۸ رقمی + قفل خودکار
- **اثر انگشت در اندروید**: اگر قفل صفحه‌ی گوشی (اثر انگشت/PIN سیستم) فعال باشد، دکمه‌ی «ورود با اثر انگشت» در صفحه‌ی قفل ظاهر می‌شود
- **نوار پایین اختصاصی موبایل** (داشبورد/تصمیم/تحلیل/تنظیمات/بیشتر)

## راهنمای سریع
- ویندوز: اجرا → More info ← Run anyway → نصب → مرورگر روی 8787 باز می‌شود (کنسول را باز نگه دارید)
- اندروید: نصب APK → اجازه‌ی منابع ناشناس → تنظیمات ← قفل برنامه ← فعال‌سازی
- ابر: تنظیمات ← همگام‌سازی ابری ← توکن Gist خود را وارد کنید
