# NEXUS HQ — Native Core v2.1
### Source (وب) → تحلیل → پروژه‌ی Native واقعی → Release Build — بدون WebView/الکترون/localhost

| فایل | حجم | توضیح |
|---|---|---|
| `NEXUS-HQ-NativeCore-Setup.exe` | ۱.۳ MB | نصب‌کننده‌ی دو-معماریه (x64/x86 خودکار) + شورتکات + Uninstaller |
| `NEXUS-HQ-NativeCore.exe` | ۱.۰ MB | اپ مستقل x64 — C++ خالص Win32/GDI |
| `NEXUS-HQ-NativeCore-x86.exe` | ۱.۰ MB | نسخه‌ی x86 |
| `NEXUS-HQ-NativeCore-1.0.0-android.apk` | ۲۷۷ KB | Java خالص — صفر مجوز، صفر WebView |

## v2.1 — رابط «همان وب» بازطراحی‌شده به Native
- **سایدبار ۲۲۸px** مطابق وب: برند با گرادیان نیلی→بنفش، نقطه‌ی وضعیت سبز «محلی · ذخیره شد»،
  ناوبری (داشبورد/مرکز تصمیم/تحلیل) + گروه «دپارتمان‌ها» + تنظیمات
- **داشبورد همان Dashboard.tsx**: ۴ کارت KPI رنگی + بخش «تمرکز امروز» + «بعداً» + نمودار ستونی
- **هاله‌ی نور پس‌زمینه** (سه شعاع رنگی مثل `body::before` وب — محاسبه‌ی پیکسلی واقعی)
- **پالت دقیق وب**: ‏#08090c / #0d0f14 / #11141b / #1e222c / #6366f1 → بازترسیم GDI
- فونت وزیرمتن ۴ وزن داخل EXE · تقویم جلالی · راست‌چین کامل

## Android
Java خالص با همان پالت تیره و فونت وزیرمتن — بدون هیچ مجوزی.

## تکنولوژی (پاسخ «تشخیص سورس»)
سورس گیت‌هاب: **React 19 + Vite + TypeScript (وب)** → معماری انتخابی برای بازنویسی:
**ویندوز: C++ / Win32 (بدون WebView)** · **اندروید: Java بومی (بدون WebView)**
دلایل: فایروال محیط، Flutter/RN/Gradle/SDK و .NET را می‌بندد؛ Win32/Java با کامپایلر موجود ۱۰۰% Native شدند.
