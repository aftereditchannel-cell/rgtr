# --- node / web ---
node_modules/
dist/

# --- خروجی electron-builder (فایل‌های نهایی .exe عمداً نگه داشته می‌شوند تا از طریق git قابل دریافت باشند) ---
release/*.blockmap
release/builder-debug.yml
release/builder-effective-config.yaml
release/win-unpacked/
release/linux-unpacked/

# --- android ---
android/.gradle/
android/build/
android/app/build/
android/.idea/
android/*.iml
android/local.properties
# این پوشه هر بار با «npm run cap:sync» ساخته می‌شود
android/app/src/main/assets/public/

# --- متفرقه ---
.DS_Store
*.log

# خروجی‌های build در git هستند (build-output/) — این خط فقط برای اطمینان از جدا ماندن سورس است

# خروجی‌های میانی build-native (قابل تولید مجدد با gen_assets.py / gen_payload.py)
build-native/win/assets.cpp
build-native/win/payload.cpp
build-native/win/*.pdb
build-apk/*.apk
build-apk/classes.dex
build-apk/AndroidManifest.xml.bin
build-apk/resources.arsc
build-apk-native/*.apk
build-apk-native/classes.dex
build-native/win-native/payload_native.cpp
